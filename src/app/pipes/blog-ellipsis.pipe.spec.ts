import { BlogEllipsisPipe } from './blog-ellipsis.pipe';

describe('BlogEllipsisPipe', () => {
  it('create an instance', () => {
    const pipe = new BlogEllipsisPipe();
    expect(pipe).toBeTruthy();
  });
});
